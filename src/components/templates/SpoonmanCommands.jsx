import React from 'react';

import { InnerBox } from './common';

const ServerCommands = () => {
  return (
    <InnerBox className="isOpen">
      <div>Test</div>
    </InnerBox>
  );
};

export default ServerCommands;
